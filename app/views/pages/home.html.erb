<div class="home-callout text-center container-fluid">
    <h1>TheSite</h1>
    <h3>All you will ever need</h3>
</div>
<div class="container home-signup-links">
    <div class="row">
        <div class="col-md-6" >
            <div class="well">
                <h2 class="text-center">Basic membership</h2>
                <h4>Sign up for free and get the basic features.</h4>
                <br/>
                <%= link_to "Sign up with Basic plan", new_user_registration_path(plan: @basic_plan.id), 
                class: 'btn btn-primary btn-lg btn-block' %>
            </div>
        </div>
        <div class="col-md-6" >
            <div class="well">
                <h2 class="text-center">Pro membership</h2>
                <h4>Sign up for pro account for $10/month 
                and get amazing features</h4>
                <br/>
                <%= link_to "Fix this stupid look", new_user_registration_path(plan: @pro_plan.id), 
                class: 'btn btn-success btn-lg btn-block' %>
            </div>
        </div>
    </div>
    <!-- current_user.id will not become query string param, because :user_id is in dynamic route --> 
    <%= link_to "Create profile", new_user_profile_path(user_id: current_user.id) if user_signed_in? %>
</div>